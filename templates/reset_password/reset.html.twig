{% extends 'base.html.twig' %}

{% block title %}Renueva tu password{% endblock %}

{% block body %}
    <h1>Renueva tu password</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Renovar password</button>
    {{ form_end(resetForm) }}
{% endblock %}
