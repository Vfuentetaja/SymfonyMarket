
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <!-- Logo -->
    <li class="nav-item active me-auto p-1">
      <img  src="{{ asset('images/' ~ 'logo-piratas.png') }}" alt="Imagen logo" class="logo-imagen"/>
    </li>


    <!-- Botón de hamburguesa para versión móvil -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
  <ul class="navbar-nav ms-auto mb-2 mb-lg-0 navbar-right">

  
       <!-- Selector de idiomas -->
    <li class="nav-item">
      {% set app_ruta = app.request.attributes.get('_route') %}
      {{ render(controller('App\\Controller\\IdiomaController::index',{ruta: app_ruta})) }}
    </li>
    <!-- Login/Logout y Registro -->
    <li class="nav-item">
      {% if app.user %}
        <div class="dropdown">
          <button class="btn carr ms-3" onclick="mostrar()">
            <a class="bi bi-cart mx-4" ></a>
          </button>
         </br> 
          <button class="btn text-black dropdown-toggle ms-3" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false" >
            {{ 'say_hello'|trans({'name': app.user.nombre}) }}
          </button>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton1">
            <li><a class="dropdown-item" href="{{ path('app_user_show', {'id': app.user.id}) }}">{{ 'Mi cuenta'|trans }}</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="/logout">{{ 'Cerrar sesión'|trans }}</a></li>
          </ul>
        </div>
      {% else %}
        <li class="nav-item">
          <a class="btn btn-warning me-3" href="/register">{{ 'Nuevo usuario'|trans }}</a>
          </br>
          <a class="btn btn-warning" href="/login">{{ 'Login'|trans }}</a> 
        </li>
      {% endif %}
    </li>
 
  </ul>
</div>
  </div>
</nav>



{#     {% block javascripts %} 
      {% if is_granted('ROLE_USER')%}    
        <script type="text/javascript" src="{{ asset('js/carrito.js') }}"></script>
      {% endif %}
    {% endblock %} #}
</nav>