<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container" id="navbarSupportedContent">
    <ul class="navbar-nav col-12 d-flex">
      <li class="nav-item active me-auto p-2">
        <img src="{{ asset('images/' ~ 'logo-piratas.png') }}" alt="Imagen logo" class="mini-imagen"/>
      </li>
      <li class="nav-item active p-2">
      {% if app.user %}
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/logout">Logout</a>
        </br>
        <a href="{{ path('app_user_edit', {'id': app.user.id}) }}" class="textos_cabecera">{{app.user.nombre}}</a>
      {% else %}
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/login">Login</a>
        </br>
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/register">Nuevo usuario</a>
      {% endif %}
      </li>
    </ul>
  </div>
  {% if app.user %}
  <ul class="listaCarrito"></ul>
  {% endif %}
</nav>