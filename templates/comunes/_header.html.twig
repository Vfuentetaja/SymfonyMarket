<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container" id="navbarSupportedContent">
    <ul class="navbar-nav col-12 d-flex">
      <li class="nav-item active me-auto p-1">
        <img src="{{ asset('images/' ~ 'logo-piratas.png') }}" alt="Imagen logo" class="logo-imagen"/>
      </li>
      <li class="nav-item active p-2">
        {% set app_ruta = app.request.attributes.get('_route') %}
        {{render(controller('App\\Controller\\IdiomaController::index',{ruta: app_ruta}))}}
        {# renderizamos directamente el controller #}
      </li> 
    
      <li class="nav-item active p-2">
      {% if app.user %}
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/logout">Logout</a>
        </br>    
        <a href="{{ path('app_user_show', {'id': app.user.id}) }}" class="textos_cabecera">{{ 'say_hello'|trans({'name': app.user.nombre}) }} </a>
      {% else %}
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/login">Login</a>
        </br>
        <a class="text-white no-underline hover:text-white hover:no-underline" href="/register">{{ 'Nuevo usuario'|trans }}</a>
      {% endif %}
      </li>
      {% if app.user %}
        <li>
          <button class="carr" onclick="mostrar()"><img src="{{ asset('images/carrito.png') }}" alt="carritocompra" class="mini-imagen"/></button>
        </li>
      {% endif %}
      
    </ul>
  </div>

{#     {% block javascripts %} 
      {% if is_granted('ROLE_USER')%}    
        <script type="text/javascript" src="{{ asset('js/carrito.js') }}"></script>
      {% endif %}
    {% endblock %} #}
</nav>