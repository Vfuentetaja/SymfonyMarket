{# <div class="dropdown">
	   <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="selectorIdioma" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ listado_idiomas[idioma_actual] }}</a>    
	   <div class="dropdown-menu" aria-labelledby="selectorIdioma">
		{% for lenguaje, nombre_lenguaje in listado_idiomas %} 
			<form action="{{path('app_idioma',{'_locale': lenguaje})}}" method="POST"> 
			     <input type="hidden" name="ruta" value="{{ ruta }}"/>
			     <button type="submit" class="btn dropdown-item {{ idioma_actual == lenguaje ? 'active' }}">{{ nombre_lenguaje }}</button>
			</form>
		{% endfor%}
	   </div>
</div> #}
<div class="idiomas">
	{% for lenguaje, nombre_lenguaje in listado_idiomas %} 
		<form action="{{path('app_idioma',{'_locale': lenguaje})}}" method="POST"> 
			<input type="hidden" name="ruta" value="{{ ruta }}"/>
			<button type="submit" class="btn dropdown-item {{ idioma_actual == lenguaje ? 'active' }}">
			{% if nombre_lenguaje=='es' %}
				<img src="{{ asset('images/' ~ 'ban_es.jpg') }}" class="mini-bandera" alt="es">
			{% else %}
				<img src="{{ asset('images/' ~ 'ban_en.jpg') }}" class="mini-bandera" alt="en">'
			{% endif %}
			</button>
		</form>
	{% endfor%}
</div>

