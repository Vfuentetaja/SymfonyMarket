{% extends 'base.html.twig' %}

{% block title %}Edit Respuesta{% endblock %}

{% block body %}
    <h1>Edit Respuesta</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Guardar') }}</button>
    {{ form_end(form) }}
{% endblock %}
