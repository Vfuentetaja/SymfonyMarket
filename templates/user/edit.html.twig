{% extends 'base.html.twig' %}

{% block title %}Actualizar usuario{% endblock %}

{% block body %}
    <div class="container contenedor">
        <div class="row">
            <div class="col-xs-12 nav-btns button">
                <h1>Actualizar usuario</h1>

                {{ include('user/_form.html.twig', {'button_label': 'Actualizar'}) }}
                <div class="contenedor-btn">
                    {% if is_granted('ROLE_ADMIN')%}
                    <a href="{{ path('app_user_index') }}"><button class="btn btn-warning">Volver</button></a>
                    {% else %}
                    <a href="{{ path('app_producto_index') }}"><button class="btn btn-warning">Volver</button></a>
                    {% endif %}
                    {{ include('user/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>



{% endblock %}
